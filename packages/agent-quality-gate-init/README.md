# agent-quality-gate-init

Thin installer for `agent-quality-gate`.

```bash
bunx agent-quality-gate-init
```

The init command installs the heavy runtime into the user cache and wires the
current project to that pinned runtime version without adding the runtime package
to the project dependencies.
